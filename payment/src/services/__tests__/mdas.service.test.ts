import { afterAll, describe, expect, jest, test } from "@jest/globals";
import MDAs from "../../models/MDAs";

jest.mock("../../models/MDAs");

describe("MDAs Service", () => {
  afterAll(() => {
    jest.clearAllMocks();
  });

  describe("Get All MDAs Service", () => {});
  describe("Register A MDAs Service", () => {});
  describe("Sign In A MDAs Service", () => {});
  describe("Get A Single MDAs Service", () => {});
  describe("Update A Single MDAs Service", () => {});
  describe("Delete A Single MDAs Service", () => {});
});
