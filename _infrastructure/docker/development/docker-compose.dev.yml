name: selleasi-dev

networks:
  selleasi-frontend:
    driver: bridge
  selleasi-gateway:
    driver: bridge
  selleasi-services:
    driver: bridge
  selleasi-data:
    driver: bridge
  selleasi-monitoring:
    driver: bridge

volumes:
  redis-data:
  grafana_data:
  loki_data:
  rabbitmq_data:
  tempo-data:
  es-data:
  mongo-local-data:
  ui_dist:
  prom_data:

services:

  #  DATA LAYER 

  mongodb:
    image: mongo:7
    container_name: selleasi_mongodb
    networks:
      - selleasi-data
    ports:
      - "27018:27017"
    volumes:
      - mongo-local-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: unless-stopped

  redis:
    image: redis:7.2-alpine
    container_name: selleasi_redis
    networks:
      - selleasi-data
    volumes:
      - redis-data:/data
    command: >
      redis-server
      --save 60 1
      --loglevel warning
      --requirepass ${REDIS_PASSWORD}
      --bind 0.0.0.0
      --protected-mode yes
      --appendonly yes
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a ${REDIS_PASSWORD} ping | grep PONG"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: selleasi_rabbitmq
    hostname: selleasi-rabbit
    networks:
      - selleasi-data
    ports:
      - "5672:5672"
      - "15672:15672"
      - "15692:15692"
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
      RABBITMQ_DEFAULT_VHOST: ${RABBITMQ_DEFAULT_VHOST}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
      - ./_infrastructure/rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf:ro
      - ./_infrastructure/rabbitmq/enabled_plugins:/etc/rabbitmq/enabled_plugins:ro
      - ./_infrastructure/rabbitmq/definitions.json:/etc/rabbitmq/definitions.json:ro
    healthcheck:
      test: ["CMD", "rabbitmqctl", "status"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 40s
    restart: unless-stopped

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: selleasi_elasticsearch
    hostname: elasticsearch
    networks:
      - selleasi-data
    ports:
      - "9200:9200"
    environment:
      - discovery.type=single-node
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
      - xpack.security.enabled=false
      - xpack.security.http.ssl.enabled=false
      - cluster.routing.allocation.disk.threshold_enabled=false
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9200/_cluster/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 60s
    volumes:
      - es-data:/usr/share/elasticsearch/data
    restart: unless-stopped

  #  MONITORING 

  tempo:
    image: grafana/tempo:latest
    user: root
    container_name: selleasi_tempo
    ports:
      - "3201:3200"
      - "4319:4318"
      - "4320:4317"
    networks:
      - selleasi-monitoring
      - selleasi-services
    command: ["-config.file=/etc/tempo/tempo.yaml"]
    volumes:
      - ./_infrastructure/tempo/tempo-config.yaml:/etc/tempo/tempo.yaml:ro
      - tempo-data:/tmp/tempo
    restart: unless-stopped

  loki:
    image: grafana/loki:2.9.4
    container_name: selleasi_loki
    ports:
      - "3101:3100"
    networks:
      - selleasi-monitoring
    command: -config.file=/etc/loki/loki-config.yml -config.expand-env=true
    volumes:
      - loki_data:/loki
      - ./_infrastructure/loki/loki-config.yml:/etc/loki/loki-config.yml:ro
    restart: unless-stopped

  promtail:
    image: grafana/promtail:2.9.4
    container_name: selleasi_promtail
    networks:
      - selleasi-monitoring
    command: -config.file=/etc/promtail/promtail-config.yml
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - ./_infrastructure/promtail/promtail-config.yml:/etc/promtail/promtail-config.yml:ro
    restart: unless-stopped
    depends_on:
      - loki

  prometheus:
    image: prom/prometheus:v2.51.0
    container_name: selleasi_prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=3d"
      - "--web.enable-lifecycle"
    ports:
      - "9091:9090"
    networks:
      - selleasi-monitoring
      - selleasi-services
    volumes:
      - ./_infrastructure/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prom_data:/prometheus
    restart: unless-stopped

  grafana:
    image: grafana/grafana:10.3.1
    container_name: selleasi_grafana
    ports:
      - "3001:3000"
    networks:
      - selleasi-monitoring
    environment:
      - GF_PATHS_PROVISIONING=/etc/grafana/provisioning
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_FEATURE_TOGGLES_ENABLE=alertingSimplifiedRouting,alertingQueryAndExpressionsStepMode
      - GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS=grafana-exploretraces-app
      - GF_AUTH_ANONYMOUS_ORG_NAME=Main Org.
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - GF_DASHBOARDS_MIN_REFRESH_INTERVAL=5s
    volumes:
      - grafana_data:/var/lib/grafana
      - ./_infrastructure/grafana/provisioning:/etc/grafana/provisioning:ro
    depends_on:
      - prometheus
      - loki
      - tempo
    restart: unless-stopped

  #  FRONTEND 

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: selleasi_frontend
    volumes:
      - ui_dist:/srv/ui
    networks:
      - selleasi-frontend
    restart: "no"

  #  GATEWAY 

  caddy:
    image: caddy:2-alpine
    container_name: selleasi_caddy
    ports:
      - "80:80"
      - "443:443"
      - "2019:2019"
    volumes:
      - ./_infrastructure/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ui_dist:/srv/ui:ro
    networks:
      - selleasi-frontend
      - selleasi-gateway
    depends_on:
      frontend:
        condition: service_completed_successfully
      api-gateway:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  api-gateway:
    build:
      context: ./backend/api-gateway
      dockerfile: Dockerfile
    container_name: selleasi_api_gateway
    env_file:
      - ./.env
      - ./backend/api-gateway/.env
    ports:
      - "127.0.0.1:8000:8000"
    networks:
      - selleasi-gateway
      - selleasi-services
      - selleasi-data
    depends_on:
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  #  CORE SERVICES 

  authentication:
    build:
      context: ./backend/authentication
      dockerfile: Dockerfile
    container_name: selleasi_authentication
    env_file:
      - ./.env
      - ./backend/authentication/.env
    ports:
      - "127.0.0.1:4001:4001"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4001/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  organization:
    build:
      context: ./backend/organization
      dockerfile: Dockerfile
    container_name: selleasi_organization
    env_file:
      - ./.env
      - ./backend/organization/.env
    ports:
      - "127.0.0.1:4010:4010"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4010/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  subscription:
    build:
      context: ./backend/subscription
      dockerfile: Dockerfile
    container_name: selleasi_subscription
    env_file:
      - ./.env
      - ./backend/subscription/.env
    ports:
      - "127.0.0.1:4017:4017"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4017/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  stores:
    build:
      context: ./backend/stores
      dockerfile: Dockerfile
    container_name: selleasi_stores
    env_file:
      - ./.env
      - ./backend/stores/.env
    ports:
      - "127.0.0.1:4007:4007"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4007/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  products:
    build:
      context: ./backend/products
      dockerfile: Dockerfile
    container_name: selleasi_products
    env_file:
      - ./.env
      - ./backend/products/.env
    ports:
      - "127.0.0.1:4003:4003"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4003/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  inventory:
    build:
      context: ./backend/inventory
      dockerfile: Dockerfile
    container_name: selleasi_inventory
    env_file:
      - ./.env
      - ./backend/inventory/.env
    ports:
      - "127.0.0.1:4008:4008"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4008/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  cart:
    build:
      context: ./backend/cart
      dockerfile: Dockerfile
    container_name: selleasi_cart
    env_file:
      - ./.env
      - ./backend/cart/.env
    ports:
      - "127.0.0.1:4009:4009"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4009/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  orders:
    build:
      context: ./backend/orders
      dockerfile: Dockerfile
    container_name: selleasi_orders
    env_file:
      - ./.env
      - ./backend/orders/.env
    ports:
      - "127.0.0.1:4012:4012"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      cart:
        condition: service_healthy
      inventory:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4012/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  payment:
    build:
      context: ./backend/payment
      dockerfile: Dockerfile
    container_name: selleasi_payment
    env_file:
      - ./.env
      - ./backend/payment/.env
    ports:
      - "127.0.0.1:4004:4004"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      orders:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4004/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  notification:
    build:
      context: ./backend/notification
      dockerfile: Dockerfile
    container_name: selleasi_notification
    env_file:
      - ./.env
      - ./backend/notification/.env
    ports:
      - "127.0.0.1:4006:4006"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4006/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  review:
    build:
      context: ./backend/review
      dockerfile: Dockerfile
    container_name: selleasi_review
    env_file:
      - ./.env
      - ./backend/review/.env
    ports:
      - "127.0.0.1:4011:4011"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4011/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  audit:
    build:
      context: ./backend/audit
      dockerfile: Dockerfile
    container_name: selleasi_audit
    env_file:
      - ./.env
      - ./backend/audit/.env
    ports:
      - "127.0.0.1:4002:4002"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4002/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  escrow:
    build:
      context: ./backend/escrow
      dockerfile: Dockerfile
    container_name: selleasi_escrow
    env_file:
      - ./.env
      - ./backend/escrow/.env
    ports:
      - "127.0.0.1:4018:4018"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4018/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  users:
    build:
      context: ./backend/users
      dockerfile: Dockerfile
    container_name: selleasi_users
    env_file:
      - ./.env
      - ./backend/users/.env
    ports:
      - "127.0.0.1:4016:4016"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4016/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  categories:
    build:
      context: ./backend/categories
      dockerfile: Dockerfile
    container_name: selleasi_categories
    env_file:
      - ./.env
      - ./backend/categories/.env
    ports:
      - "127.0.0.1:4005:4005"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4005/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  color:
    build:
      context: ./backend/color
      dockerfile: Dockerfile
    container_name: selleasi_color
    env_file:
      - ./.env
      - ./backend/color/.env
    ports:
      - "127.0.0.1:4013:4013"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4013/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  size:
    build:
      context: ./backend/size
      dockerfile: Dockerfile
    container_name: selleasi_size
    env_file:
      - ./.env
      - ./backend/size/.env
    ports:
      - "127.0.0.1:4015:4015"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4015/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  view:
    build:
      context: ./backend/view
      dockerfile: Dockerfile
    container_name: selleasi_view
    env_file:
      - ./.env
      - ./backend/view/.env
    ports:
      - "127.0.0.1:4014:4014"
    networks:
      - selleasi-services
      - selleasi-data
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4014/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped